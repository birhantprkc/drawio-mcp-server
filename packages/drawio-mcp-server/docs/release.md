# Release

The release procedure for this package lives in the monorepo-wide guideline: [docs/release.md](../../docs/release.md) at the repository root. It covers version bumps (including the `VERSION` constant in `src/index.ts`), the README "Key Highlights" chips, syncing this package's README for the npm registry, the `pnpm pack` smoke test, tagging, and what each publish workflow expects.

This file is kept only as a pointer so that the package-local `docs/` directory resolves.